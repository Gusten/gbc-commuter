<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="pub.gusten.gbgcommuter.MainActivity"
    tools:showIn="@layout/activity_main">

    <AutoCompleteTextView
        android:id="@+id/main_from_location"
        android:layout_width="match_parent"
        android:layout_height="50dp" />

    <AutoCompleteTextView
        android:id="@+id/main_to_location"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/main_from_location" />

    <ListView
        android:id="@+id/available_routes_list"
        android:layout_width="match_parent"
        android:layout_height="390dp"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toBottomOf="@+id/main_to_location"></ListView>

</android.support.constraint.ConstraintLayout>
